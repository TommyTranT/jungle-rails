    <%# This is a PARTIAL that has code taken from products/show %>
    <%# Changed @product to product from the show file %>

    <div> 
      <h1><%= product.name %></h1>
      <p><%= product.description %></p>
      <div class="quantity">
        <span><%= product.quantity %> in stock at </span>
        <span><%= humanized_money_with_symbol product.price %></span>
      </div>
      <div class="price">          
        <%= button_to add_item_cart_path(product_id: product.id), class: 'btn' , method: :post do %>
          <%= fa_icon "shopping-cart", text: 'Add' %>
        <% end %>          
      </div>
    </div>